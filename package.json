{
  "name": "novarouter",
  "version": "2.0.0",
  "private": true,
  "description": "NovaRouter — AI gateway (FastAPI + Jinja2 UI). `bun run dev` starts the Python server; bun itself hosts only the NovaFree engine sidecar.",
  "scripts": {
    "dev": "python3 main.py 2>&1 | tee dev.log",
    "dev:python": "python3 main.py",
    "start": "python3 main.py 2>&1 | tee server.log",
    "db:push": "python3 -c \"from nova.bootstrap import run_bootstrap; run_bootstrap()\"",
    "lint": "python3 -m compileall -q main.py nova routers ui && echo lint-ok"
  }
}
